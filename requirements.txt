fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
PyJWT==2.10.1
bcrypt==4.2.1
python-dateutil==2.9.0.post0
google-genai==0.5.0
pypdf==5.1.0
httpx==0.28.1

# Postgres driver. Unused with the default SQLite URL, but present so that
# switching DATABASE_URL to postgresql+psycopg://... needs no redeploy config.
psycopg[binary]==3.2.3

# dev / verification
pytest==8.3.4
